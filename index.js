function reverseStr(){
var inputStr = prompt("Enter string for reversal");
    if(true){
        confirm("Do you want to proceed?");
        if(true)
            var reverseStr = inputStr.split('').reverse().join('');
            alert("Reversed string: "+ reverseStr);
    }
}